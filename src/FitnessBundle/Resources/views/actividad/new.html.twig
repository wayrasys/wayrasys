{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
    <h1>Nueva Actividad</h1>
    </div>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-default" value="Guardar" />
        <a class="btn btn-default" href="{{ path('actividad_index') }}">Volver</a>
    {{ form_end(form) }}
{% endblock %}
